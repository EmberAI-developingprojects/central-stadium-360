# Dependencies
node_modules/
**/node_modules/

# Build output
dist/
build/
.vite/

# Environment + secrets
.env
.env.*
!.env.example

# Editor / OS noise
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# Local agent state
.claude/

# Large media (exceeds GitHub's 100 MB per-file limit).
# Kept on disk locally — use Git LFS or host externally if other devs need it.
frontend/public/assets/video/our-story.mp4
