# Local dev
VITE_API_BASE_URL=http://localhost:3000

# Supabase ── Project Settings → API ─────────────────────────────────────
# The anon key is safe to ship to the browser; the service key never is.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
