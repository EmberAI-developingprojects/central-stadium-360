// Events, home content, and users now all live in Supabase. The only thing
// still using localStorage is the legacy orders mock pending its migration —
// no seed data needed for that path. This file is kept as a placeholder so
// existing imports don't break.

export {};
